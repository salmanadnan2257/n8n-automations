# Credentials required

- Supadata API key (used as the `x-api-key` header on the HTTP Request node calling `https://api.supadata.ai/v1/youtube/transcript`). Sign up at Supadata to get one.
- An n8n instance (self-hosted or cloud) to host and run the webhook.
- No other third-party service, database, or credential is used by this workflow.
